[project]
name = "faultline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "networkx>=3.3",
    "fastapi>=0.115",
    "uvicorn>=0.30",
]
